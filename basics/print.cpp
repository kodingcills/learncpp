#include <iostream> // preprocessor directive to use contents of iostream library
//c++ library that allows to read and write test from/to console

int main(){ // every cpp has to have main or will fail to link, when ran, the statements inside are executed in sequential order
    std::cout << "Hiiiiiiiii"; // used iostream
    int x;
    
    return 0; // when executable program is done, the program send s a value back tot he oeprating system to indicate whether it ran successfully or not.
    // basically, it tells the compilter that the program has completed successfully.
}